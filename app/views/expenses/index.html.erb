
<div class="container-fluid ">
    <div class="col-md-12">
      <div class="card">
        <div class="card-body">
          <h3 class="card-title">Mis Gastos</h3>
          <br>
           <ul lass="list-group">
            <% if @expenses.any? %>
              <% @expenses.each do |expense| %>
                <li class="list-group-item"><%= expense.category %> $<%= expense.amount %></li>
              <% end %>
            <% else %>
              <li>Aun no tienes gastos</li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>
</div>
